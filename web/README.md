# Web (React + Vite) - refactor

## Ejecutar
```bash
cd web
npm install
npm run dev
```
Abre: http://localhost:5173

Configuración opcional:
- `VITE_API_BASE`: URL base del backend (default `http://localhost:3000`)
